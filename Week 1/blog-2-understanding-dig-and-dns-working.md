# Understanding DIG and DNS Working

DNS looks simple on the surface, but what actually happens **after you type a domain in the browser**?

This blog uses the `dig` command to trace DNS resolution step by step — from root servers to authoritative name servers — and shows how a domain name is resolved behind the scenes.

If you’ve ever wondered *where DNS queries go*, *what root and TLD servers actually do*, or *how recursion works in real life*, this blog walks through the entire journey practically.

👉 Read the full blog here:  
https://bitbybitblog.hashnode.dev/understanding-dig-and-dns-working